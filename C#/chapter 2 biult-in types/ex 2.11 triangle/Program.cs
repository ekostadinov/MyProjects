using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ex_2._11_triangle
{
    class Program
    {
        static void Main(string[] args)
        {
            string symbol = "©";
            Console.WriteLine(symbol);
            Console.WriteLine(symbol + symbol);
            Console.WriteLine(symbol + symbol + symbol);
            Console.WriteLine(symbol + symbol + symbol + symbol);
            Console.WriteLine(symbol + symbol + symbol + symbol + symbol);
            Console.WriteLine(symbol + symbol + symbol + symbol + symbol + symbol);
            Console.WriteLine(symbol + symbol + symbol + symbol + symbol + symbol + symbol);
            Console.WriteLine(symbol + symbol + symbol + symbol + symbol + symbol + symbol + symbol);
            Console.WriteLine(symbol + symbol + symbol + symbol + symbol + symbol + symbol + symbol + symbol);
        }
    }
}
